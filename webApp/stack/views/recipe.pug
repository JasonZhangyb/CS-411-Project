extends layout

block content
    a(href="/main") Go back to main page
    a(href='/auth/logout') Log out Twitter
    h1= title

    for results in result.hits
        p #{results.recipe.label}
        p
        a(href=results.recipe.url) Find the recipe
        p
        img(src=results.recipe.image)
        form(action='/restaurant', method = 'post') Find nearby restaurant:
            input(type='text', name='search', placeholder=' ')
            input(type='submit', value=results.recipe.label, name="label", action='/restaurant')
